# Diagnostic - paste the JSX hierarchy of the ImmediateDifference section

The dark card in the AN IMMEDIATE DIFFERENCE section is still stretching to match the height of the right-column image. I have asked for `items-start` and `h-fit` in two prior rounds and the stretching persists. There must be something else forcing the height. Help me find it by reporting the actual current state of the file - no modifications.

Open `src/components/ImmediateDifference.tsx` (or whatever the file is called for that section) and paste back the FULL component file. Include:
- All imports
- The complete component function
- Every className on every JSX element from the outermost wrapper down to the 4 use-case row items

Do not summarize. Do not modify. Just paste the file's contents verbatim from the source.

If the section is broken across multiple files (e.g. parent layout, then ImmediateDifference, then sub-components), paste all of them.

I need to see the actual class strings as they currently exist so I can identify which parent is forcing the height.
